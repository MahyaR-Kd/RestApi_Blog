const Pool = require('pg').Pool;

const pool = new Pool({
    user:"database user",
    password:"database password",
    database:"database name",
    host:"127.0.0.1",
    port:"5432"
  });
  

  module.exports = pool;